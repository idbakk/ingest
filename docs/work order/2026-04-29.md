Yes. Here is the **revision order** I recommend.

## Target generation order

Locked order:

```text id="mhvgoc"
1. write manifest.json
2. run checksum/media/media-policy validation
3. write asset_report.json
4. write ai_report.json
5. write ingest_report.json
6. write ingest_report.html
7. optionally write ingest_findings.csv
```

This matches the new report note you added. 

## Important implementation point

Current ASL order is still:

```text id="ysxuvj"
asset_report.json → report.json → ai_report.json
```

The current Step Functions file shows `WriteAssetReport...` going to `WriteReport...`, then `WriteAiReport...`. 

We need to change it to:

```text id="q5vpo8"
asset_report.json → ai_report.json → ingest_report.json/html/csv
```

But the current AI report Lambda expects the deterministic `report_s3_uri` as input. So the cleanest update is:

> revise `ingest-write-ai-report` so it can receive deterministic summary data directly from Step Functions, instead of depending on the old `report.json`.

That avoids creating an extra intermediate report file.

---

# Revision order

## 1. Create safety branch

```bash id="a9h8gc"
cd ingest
git status
git checkout -b v1-1-ingest-report-html
```

## 2. Update `ingest-write-ai-report`

Goal: AI report should no longer require the old `report_s3_uri`.

Current input model:

```json id="bjvpml"
{
  "job_id": "...",
  "project_code": "...",
  "report_s3_uri": "...",
  "asset_report_s3_uri": "..."
}
```

New input model:

```json id="kqs8jb"
{
  "job_id": "...",
  "project_code": "...",
  "ruleset_version": "v1.0",
  "trigger": "_INGEST_DONE",
  "ingest_folder": "s3://...",
  "manifest_s3_uri": "s3://...",
  "final_state": "READY_FOR_REVIEW",
  "deep_validation_summary": {},
  "validate_result": {},
  "asset_report_s3_uri": "s3://..."
}
```

It can still read `asset_report.json`, but the deterministic summary should come from the ASL payload.

This preserves the AI rule: AI is advisory only, does not write job state, and does not override deterministic findings. 

## 3. Update `ingest-write-report`

Goal: this Lambda becomes the **final report writer**.

It should write:

```text id="spdz7u"
_reports/<job_id>/ingest_report.json
_reports/<job_id>/ingest_report.html
_reports/<job_id>/ingest_findings.csv   # optional
```

It should receive:

```json id="eg61hw"
{
  "job_id": "...",
  "project_code": "...",
  "ruleset_version": "v1.0",
  "trigger": "_INGEST_DONE",
  "ingest_folder": "s3://...",
  "manifest_s3_uri": "s3://...",
  "final_state": "READY_FOR_REVIEW",
  "deep_validation_summary": {},
  "validate_result": {},
  "asset_report_s3_uri": "s3://...",
  "ai_report_s3_uri": "s3://..."
}
```

It should build `ingest_report.json` with deterministic sections first and `ai_advisory` at the bottom, as locked in your report note. 

## 4. Update ASL order

For each route, change this:

```text id="qy8dn4"
WriteAssetReportReadyForReview
  → WriteReportReadyForReview
  → WriteAiReportReadyForReview
  → Done
```

To this:

```text id="ffxniu"
WriteAssetReportReadyForReview
  → WriteAiReportReadyForReview
  → WriteIngestReportReadyForReview
  → Done
```

And this:

```text id="5ucqmd"
WriteAssetReportRejectedPolicy
  → WriteReportRejectedPolicy
  → WriteAiReportRejectedPolicy
  → Done
```

To this:

```text id="bsm3xy"
WriteAssetReportRejectedPolicy
  → WriteAiReportRejectedPolicy
  → WriteIngestReportRejectedPolicy
  → Done
```

## 5. Update DynamoDB report pointer fields

Current fields can stay for compatibility, but add clearer final-report fields:

```text id="muz9xe"
ingest_report_json_s3_uri
ingest_report_html_s3_uri
ingest_findings_csv_s3_uri
ingest_report_generated_at
```

Keep existing fields temporarily:

```text id="hixado"
report_s3_uri
report_bucket
report_key
report_generated_at
```

For backward compatibility, `report_s3_uri` can point to `ingest_report.json`.

## 6. Regression test

After deployment, rerun minimum smoke set:

```text id="lz4551"
R1 clean no-MHL baseline
R5 unreadable media / policy reject
```

That is the required minimum after reporting/ASL edits. 

---

# Start now

First, create the branch and show me the current two handler files:

```bash id="h897vz"
cd ingest
git status
git checkout -b v1-1-ingest-report-html
```

Then open or paste:

```text id="qg8z7m"
lambdas/ingest-write-report/handler.py
lambdas/ingest-write-ai-report/handler.py
```

We should update **`ingest-write-ai-report` first**, then **`ingest-write-report`**, then the **ASL**.
